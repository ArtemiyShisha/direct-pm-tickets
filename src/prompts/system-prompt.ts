import { CRITERIA, CRITERIA_GROUPS } from "@/lib/types";

const BASE_PROMPT = `Ты проводишь ревью эпика перед приёмкой в бэклог Яндекс Директа. Твоя задача — найти все пробелы, из-за которых разработка может застопориться или результат окажется непредсказуемым.

ПРАВИЛА:
- Оценивай СТРОГО по тексту. Не додумывай.
- Если информация отсутствует — это 0-2 балла.
- Будь строгим: завышенная оценка вредит команде.

ШКАЛА (0-10):
0-1 = отсутствует | 2-3 = упомянуто без конкретики | 4-5 = попытка с пробелами | 6 = базовый уровень | 7-8 = хорошо | 9-10 = отлично

ДЛЯ КАЖДОГО КРИТЕРИЯ:
1. analysis — Выпиши что нашёл в тексте: цитаты, факты. Отметь чего не хватает. Это твой chain-of-thought.
2. score — Оценка 0-10 строго на основе analysis.
3. comment — Краткий вывод: что не хватило (или почему оценка высокая).
4. questions — Конкретные вопросы к PM по пробелам этого критерия. Если пробелов нет — пустой массив.
5. suggestion — Если score < 7: предложи черновик текста, который PM может вставить в эпик. Если score >= 7: null.`;

const CRITERIA_DESCRIPTIONS: Record<string, string> = {
  problem:
    "Проблема пользователя/бизнеса, кто страдает, масштаб (тиры, доля выручки)",
  solution: "Как именно решаем проблему, суть подхода",
  potential:
    "Потенциал выручки или иного результата с цифрами/обоснованием",
  metrics: "Основная и доп. метрики с целевыми значениями",
  analytics:
    "Данные, подтверждающие гипотезы (исследования, цифры, A/B)",
  design: "Макеты в Figma с покрытием основных состояний",
  scenarios:
    "Пользовательские сценарии: шаги, состояния UI, реакция системы",
  corner_cases: "Граничные случаи и реакция системы на них",
  onboarding:
    "Онбординг для новых и существующих пользователей, конкретные механики",
  interfaces:
    "Какие интерфейсы затронуты (веб десктоп, тач, моб. приложение, API, Коммандер, Excel) + обоснование если не все",
  international:
    "Страны и языки поддержки + обоснование ограничений",
  ready_for_dev:
    "Достаточно ли описания для старта разработки бэкенда и фронтенда без доп. уточнений",
  logging:
    "Требования к логированию: что логировать, формат, retention",
  launch:
    "Сценарий раскатки, сетап эксперимента/бета, критерии приёмки, план отката",
};

const GROUP_EXAMPLES: Record<string, string> = {
  business: `ПРИМЕР:

Текст: «Проблема затрагивает рекламодателей Tier-1 (>100K руб/мес) — 2400 аккаунтов, 34% выручки. Они не могут массово редактировать ставки.»

→ problem:
  analysis: "В тексте указан сегмент Tier-1, порог >100K руб/мес, количество аккаунтов (2400) и доля выручки (34%). Описана конкретная боль — невозможность массового редактирования ставок."
  score: 9
  comment: "Чётко описана проблема, указан сегмент и доля выручки"
  questions: []
  suggestion: null

Текст: «Пользователям неудобно работать со ставками»

→ problem:
  analysis: "Упомянута абстрактная проблема без указания сегмента, количества пользователей и влияния на бизнес."
  score: 3
  comment: "Проблема абстрактная: нет сегмента, масштаба, данных о влиянии"
  questions: ["Какой сегмент рекламодателей затронут?", "Сколько аккаунтов сталкиваются с проблемой?", "Какая доля выручки приходится на них?"]
  suggestion: "Проблема затрагивает рекламодателей [тир/сегмент] с бюджетом от [X] руб/мес — это [N] аккаунтов и [Y]% выручки. Они не могут [конкретное действие], что приводит к [последствие]."`,

  ux: `ПРИМЕР:

Текст: «Пользователь открывает список кампаний → выбирает кампанию → видит таблицу групп → нажимает "Массовое редактирование" → появляется модальное окно с полями ставок → заполняет → нажимает "Применить" → видит подтверждение.»

→ scenarios:
  analysis: "Описан пошаговый сценарий основного flow: навигация → выбор → действие → подтверждение. Указаны состояния UI (таблица, модальное окно, подтверждение)."
  score: 7
  comment: "Основной сценарий описан хорошо, но нет альтернативных путей и состояний ошибок"
  questions: ["Что видит пользователь при ошибке сохранения?", "Есть ли возможность отмены после применения?"]
  suggestion: null`,

  technical: `ПРИМЕР:

Текст: (раздел логирования отсутствует)

→ logging:
  analysis: "В тексте эпика не найдено упоминаний логирования, метрик производительности или требований к мониторингу."
  score: 0
  comment: "Раздел логирования полностью отсутствует"
  questions: ["Какие пользовательские события нужно логировать?", "Нужны ли метрики производительности (latency, error rate)?", "Какой retention для логов?"]
  suggestion: "Логирование:\\n- События: [создание/редактирование/удаление ставок, ошибки валидации]\\n- Формат: structured JSON\\n- Retention: [N] дней\\n- Метрики: [latency p50/p99, error rate, success rate]"`,
};

export function buildGroupPrompt(groupId: string): string {
  const group = CRITERIA_GROUPS.find((g) => g.id === groupId);
  if (!group) throw new Error(`Unknown group: ${groupId}`);

  const criteriaBlock = group.criteriaIds
    .map((id) => {
      const meta = CRITERIA.find((c) => c.id === id);
      const desc = CRITERIA_DESCRIPTIONS[id];
      return `- ${id} (${meta?.label}) — ${desc}`;
    })
    .join("\n");

  const example = GROUP_EXAMPLES[groupId] ?? "";

  return `${BASE_PROMPT}

ТВОЯ ГРУППА КРИТЕРИЕВ: ${group.label}

Оцени ТОЛЬКО следующие критерии:
${criteriaBlock}

${example}

НАПОМИНАНИЕ: Оценивай только по тексту. Отсутствие информации = низкий балл. Не додумывай. suggestion пиши только при score < 7.`;
}
